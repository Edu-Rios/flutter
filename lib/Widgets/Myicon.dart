import 'package:flutter/material.dart';

class Myicon extends StatelessWidget {
  const Myicon({super.key});

  @override
  Widget build(BuildContext context) {
    return Icon(Icons.star, color: Colors.blue, size: 50);
  }
}
